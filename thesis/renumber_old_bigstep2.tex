\judgbox{\hci,\pth,u,i\vdash\env\ppnd(\hci',\env')}{
  Hole instance numbering in hole environment $\sigma$ with HII $\hci$
}
\begin{mathpar}
  \Infer{\ppndl-TrivEnv}{}{\hci,\pth,u,i\vdash\varnothing\ppnd(\hci,\varnothing)}
  \and
  \Infer{\ppndl-Env}{
    \hci,\pth,u,i\vdash\env\ppnd(\hci',\env') \\
    \hci',(\pth,(x,(u,i)))\vdash d\ppnd(\hci'',d') \\
  }{\hci,\pth,u,i\vdash\env,x\leftarrow d\ppnd(\hci'',(\env',x\leftarrow d'))}
\end{mathpar}

\judgbox{\hci,\pth,u,i\vdash\env\ppns(\hci',\env')}{
  Hole instance numbering in hole environment $\sigma$ with HII $\hci$
}
\begin{mathpar}
  \Infer{\ppnsl-TrivEnv}{}{\hci,\pth,u,i\vdash\varnothing\ppns(\hci,\varnothing)}
  \and
  \Infer{\ppnsl-Env}{
    \hci,\pth,u,i\vdash\env\ppns(\hci',\env') \\
    \hci',(\pth,(x,(u,i)))\vdash d\ppns(\hci'',d') \\
  }{\hci,\pth,u,i\vdash\env,x\leftarrow d\ppns(\hci'',(\env',x\leftarrow d'))}
\end{mathpar}

\judgbox{d\ppn(\hci',\env')}{
  Hole instance numbering in expression $d$ and subexpressions
}
\begin{mathpar}
  \Infer{PP$_i$-Root}{
    \varnothing,\varnothing\vdash d\ppnd(\hci,d') \\
    \hci,\varnothing\vdash d'\ppnd(\hci',d'')
  }{d\ppn(\hci',d'')}
\end{mathpar}


%%% Local Variables:
%%% mode: latex
%%% TeX-master: "main"
%%% End:
